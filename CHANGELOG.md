# 更新日志 (CHANGELOG)

本文档记录析镜 LensAnalysis 的所有重要变更。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

---

## [1.1.0] - 未发布

### 新增

- **缓存目录实时校验**：设置页输入缓存目录时即时判断路径格式是否正确，未设置时直接显示当前系统的 Volatility 默认缓存目录（Windows 为 `%APPDATA%\volatility3`，其他系统为 `~/.cache/volatility3`）
- **缓存目录一键打开**：缓存目录配置新增「打开目录」入口，可直接在系统文件管理器中打开已填写的缓存目录，目录不存在时自动创建

### 修复

- 修复系统里找不到 `vol` 命令时静默返回空分析结果的问题：现在自动改用可用的 Python 解释器直接调用 Volatility 3，两者都不存在时明确报错并提示安装方式，不再把失败伪装成空结果
- 修复使用自定义 Python 时以 `python -m volatility3` 方式调用失败的问题（Volatility 3 没有 `__main__` 入口），改为通过启动代码直接调用；自定义 `vol` 与自定义 Python 的优先级保持不变
- 修复打包环境中 Volatility 3 已内置、却仍回退到系统里并不存在的 `vol` 命令的问题
- 修复选择缓存目录对话框返回结构变更后取不到所选路径、失败时也无提示的问题
- 修复微信分析模块缺少解释器时抛出无意义异常的问题，与主流程统一调用和报错方式
- 修复 CMD 命令历史（`cmdscan`）和控制台（`consoles`）插件只显示 2 条空记录的问题：此前树形子行被层级前缀（`*` / `**`）干扰全部丢弃，实际执行过的命令（如 7z 打包、cd 切目录）一律不显示
- 修复 Windows 中文系统下插件结果包含无法编码字符（如乱码命令历史）时 Volatility 文本渲染直接崩溃的问题：自动改用 JSON 渲染器重试（纯 ASCII 输出，不受控制台编码影响），覆盖文件扫描、CMD 命令历史和控制台插件，打包版和源码版同样生效
- 修复 macOS 从缓存加载镜像后符号表匹配状态丢失的问题
- 修复已加载镜像缺少符号表时未自动打开符号表管理器的问题

### 优化

- CMD 命令历史和控制台表格按树形层级缩进显示属性列，实际执行的命令行高亮标识，关键命令一目了然
- 缓存目录说明明确共享 `identifier.cache` 的前提是符号表存放位置也一致，并提示保存后需重启生效
- 镜像类型识别统一使用当前配置的符号表目录和缓存目录，避免与插件执行环境不一致

---

## [1.0.9] - 2026-09-15

### 新增

- 原激活页面升级为开源欢迎页，移除机器码和激活码流程，首次使用输入个人 ID 后即可进入析镜
- 个人 ID 支持中文、英文、Emoji 和特殊符号，仅保存在本地；完成欢迎页后再次启动不再重复显示
- 欢迎页新增开源项目入口，并展示多平台镜像分析、分析结果缓存快速加载、小析 AI 智能取证分析和自动制作 Linux 符号表四项核心特点

### 修复

- 修复从 `windows.psscan` 结果提取进程时错误调用 `memmap` 的问题，改为通过 PsScan 提取目标进程的主程序 PE，并明确区分「进程可执行文件」和「完整进程内存」
- 进一步修复 Windows 文件提取的地址类型判断，优先按照 `filescan` 返回的虚拟地址提取，无结果时自动回退物理地址
- 修复提取文件后沿用 Volatility 内部生成名称的问题，自动恢复为原始文件名，兼容中文名称并避免同名文件互相覆盖

### 优化

- 优化镜像缓存识别机制，由完整文件 SHA-256 改为固定分段读取共 8 MB 的内容指纹，大幅降低大型镜像首次加载等待时间
- 镜像改名、移动或复制后仍可复用同一份分析缓存；升级后旧缓存不再参与匹配，每个镜像首次加载时重新建立新版缓存
- 界面、AI 上下文和取证报告统一显示缓存指纹，不再触发完整镜像哈希计算
- 优化 Windows 符号表检查，根据当前镜像识别出的 PDB、GUID 和 Age 定点查找匹配文件，避免加载时遍历全部符号表目录
- 一键安装和项目依赖统一固定使用 Volatility 3 2.27.0，避免自动升级到新版本带来的兼容性波动

---

## [1.0.8] - 2026-08-03

### 修复

- 修复打包环境中自定义 Python、Volatility 及缓存目录的检测和执行环境不一致问题
- 修复符号表状态误判，防止其他镜像的索引文件被当成当前镜像的匹配符号表
- 修复文件提取、进程转储、DLL、ELF 和 Linux 页缓存导出的输出识别、失败传递及跨系统路由问题
- 修复 `psscan` 结果转储失败时原因不明确，以及重复提交导出任务的问题
- 修复 Windows 中文文件路径在插件执行、解析、缓存和导出链路中变成问号的问题

### 优化

- 导出文件直接写入用户选定目录，按目录串行化并通过执行前后快照识别本次产物
- 统一 AI 与图形界面的提取目录、任务取消和错误状态处理

---

## [1.0.7] - 2026-07-11

### 新增

- 新增小析 AI 内存取证助手，支持多模型配置、镜像对话历史、插件调用、符号表安装和本地证据分析

### 优化

- 优化 AI 对话、历史搜索、自动确认、任务状态和大结果渲染体验
- 增强 Windows、Linux、macOS 自定义符号表目录和本地符号表导入兼容性

### 修复

- 修复符号表匹配、插件执行、文件扫描可视化及跨平台依赖环境中的多项问题

---

## [1.0.6] - 2026-06-04

### 新增

- **自定义 Python 路径**：设置页面新增 Python 路径配置，支持切换不同 Python 版本（如 Homebrew Python、免安装版 Python），可指定可执行文件或安装目录（自动查找），保存后自动提示重启
- **自定义符号表目录**：设置页面新增符号表目录配置，支持统一目录和按操作系统（Windows/Linux/macOS）分别指定两种模式，多版本工具可共享同一套符号表，节省硬盘空间，保存后自动提示重启
- **自定义 Volatility 3 缓存目录**：设置页面新增缓存目录配置，支持共享 vol3 的 identifier.cache，多个 vol3 工具共享同一缓存可避免重复扫描符号表，加快符号查找速度，保存后自动提示重启
- **启动时依赖检测**：切换自定义 Python 后启动时自动检测依赖完整性，缺失时弹出提示引导安装
- **智能 pip 安装**：安装依赖时自动处理 PEP 668（externally-managed-environment）限制，兼容 Homebrew Python、系统 Python 等各种环境
- 导出文件、导出进程内存、提取 DLL、批量下载、提取事件日志、导出证书、导出报告、导出数据等所有保存文件操作完成后，成功提示和消息通知新增「打开目录」按钮，点击可一键在系统文件管理器（macOS Finder / Windows 资源管理器 / Linux 文件管理器）中打开文件所在目录
- 高级筛选支持同列多条件：同一列添加多个筛选条件时，匹配任一条件即显示（OR 逻辑），不同列之间仍为同时满足（AND 逻辑），可用于同时筛选多个进程名进行对比
- 优化高级筛选对话框样式，下拉框、输入框、按钮等组件统一圆角设计，添加 hover/focus 交互效果

### 修复

- 修复 Windows 符号表下载时旧版 Python SSL 握手失败的问题（强制 TLS 1.2）
- 修复选择目录对话框报错无法打开的问题（pywebview 6.x API 变更）
- 修复导出文件、导出进程内存、提取 DLL、导出证书等操作日志中无意义的"0 条记录"警告信息
- 修复消息通知中长文件名导致出现滚动条的问题
- 修复打包环境下 pypykatz 依赖偶尔误报为未安装的问题（统一 Python 命令选择逻辑）
- 修复自定义 Python 路径场景下 `_check_plugin_dependencies` 变量未定义导致的崩溃

### 优化

- 导出文件、导出进程内存等操作不再重复检测操作系统类型，直接复用镜像加载时已识别的 OS 信息，大幅提升导出速度
- 日志文件改为自动切割，单个文件上限 1MB，最多保留 3 个备份，避免日志过大占用磁盘空间
- 统一 Python 命令选择逻辑（`_get_python_cmd()`），所有依赖检测、安装、插件运行均使用同一出口，避免环境不一致
- 自定义符号表目录同时影响符号下载、符号状态展示和 vol 命令的符号查找路径
- 自定义 Python 路径同时影响 vol 命令查找：设置自定义 Python 后自动从该 Python 对应目录查找 vol 可执行文件

---

## [1.0.0] - 2026-02-02

### 新增
- 首次发布析镜 LensAnalysis
- 基于 Volatility 3.27.0 框架
- 支持 Windows、Linux、macOS 内存镜像分析
- 图形化用户界面（基于 PyWebView）
- 自动符号表下载和管理
- 代理支持（HTTP/HTTPS/SOCKS5）
- 智能缓存机制
- 分析结果导出（Markdown/HTML/Word）

### Windows 分析功能
- 进程分析：pslist, pstree, psscan, dlllist, handles, cmdline, consoles
- 网络分析：netscan（含时间戳）, netstat
- 注册表：hivelist, printkey
- 文件系统：filescan, files
- 恶意代码：malfind, ldrmodules
- 密码提取：hashdump, lsadump, cachedump
- 系统信息：svcscan, getsids, envars, timers, callbacks, ssdt, verinfo

### Linux 分析功能
- 进程分析：pslist, pstree, psscan, psaux
- 网络分析：netstat, sockstat, ip_addr, ip_link
- 文件系统：lsof, elfs, mountinfo
- 内核模块：lsmod, check_modules
- 命令历史：bash
- 环境变量：envars
- 内存分析：malfind, vmayarascan, maps

### macOS 分析功能
- 进程分析：pslist, pstree, psaux
- 网络分析：netstat, ifconfig, socket_filters
- 文件系统：lsof, list_files, mount
- 内核扩展：lsmod
- 系统检查：check_syscall, check_sysctl, check_trap_table
- 日志分析：dmesg, kevents, vfsevents

### CTF 专用功能
- Flag 自动搜索（支持多种常见格式）
- 正则表达式搜索
- 字符串提取
- 搜索历史记录

### 符号表管理
- Windows 符号表自动下载（绕过 Volatility3 PDB 解析 bug）
- Linux/macOS 符号表从 GitHub 自动下载
- 符号表状态查看
- 手动安装符号表

### 技术特性
- 跨平台支持（Windows/macOS/Linux）
- 代理配置和测试
- 跨平台临时文件处理
- 异步任务处理
- 结果缓存和复用
- 详细日志记录

---

## 版本说明

### 版本号格式

`主版本号.次版本号.修订号`

- **主版本号**：不兼容的 API 变更
- **次版本号**：向下兼容的功能新增
- **修订号**：向下兼容的问题修复

### 更新类型

- **新增** (Added): 新功能
- **变更** (Changed): 功能变更
- **弃用** (Deprecated): 即将移除的功能
- **移除** (Removed): 已移除的功能
- **修复** (Fixed): 问题修复
- **安全** (Security): 安全相关修复

---

## 路线图

### 计划中的功能 (Future)

- [ ] 更多自定义插件开发
- [ ] 内存镜像比对功能
- [ ] 自动化分析流程
- [ ] 云端符号表缓存
- [ ] 协作分析功能
- [ ] 插件市场

---

**析镜 LensAnalysis** - 专业的内存取证分析工具
